Instalacja:
1. Pobierz ze strony git program git na odpowiedni¹ platformê (np. Windows)
2. Zainstaluj (dalej, dalej, dalej...)

Konfiguracja:
1. Za³ó¿ konto na stronie GitHub
2. Na stronie GitHub za³ó¿ nowe repozytorium
3. Utwórz katalog na kompie z projektem
4. W tym katalogu PPM i otwórz konsolê Git
5. git init
6. git config --global user.name "nazwa konta na stronie GitHub"
7. git config --global user.email "emai podany podczas rejestracji"
8. git init
9. cd ~/.ssh (Ten katalog musi istnieæ, jeœli nie istnieje to nale¿y go utworzyæ. Z poziomu Windowsa mo¿e to yæ nie mo¿liwe, wiêc w kosoli wpisujemy cd ~ a póŸniej mkdir .ssh a póŸniej cd .ssh)
10. ssh-keygen -t rsa -C ares_email
11. podaj nazwê klucza
12. podaj has³o do klucza
13. wchodzimy w widowsie do tego folderu .ssh (on jest w katalogu domowym u¿ytkownika i tam jest ten klucz z roszerzeniem .pub -> otwieramy do w notepad++ i kopiujemy zawartoœæ)
14. Wchodzimy na stronê GitHuba w ustawienia w zak³adkê SSK Keys
15. Dodajemy nowy klucz SSH
16. Jakaœ nazwa klucza, a poni¿ej wklejamy to z punktu 13
17. W konsoli git wpisujemy ssh-agent
18. eval "$(ssh-agent)"
19. ssh-add nazwa_klucza_z_punktu_11
20. Podaj has³o z punktu 12
21. ssh -T git@github.com (mo¿e byæ konieczne potwierdzenie poprzez wpisanie yes)

Obs³uga nowego repozytorium:
1. Wchodzimy do katalogu projektu -> PPM otwórz konsolê Git
2. git init
3. git add .
4. git commit -m "Wiadomoœæ o czym jest commit"
5. git remote add origin link_ze_strony_GitHub_z_repozytorium
6. git push -u origin master (podajemy login i has³o/albo i nie podajemy)

Dodanie .gitignore:
1. Wchodzimy do katalogu projektu -> PPM otwórz konsolê Git
2. git init
3. touch .gitignore
4. otwiramy utworzony w³aœnie plik .gitignore przez Notepad++ i wpisujemy to co uploadowanie na serwer ma pomijaæ czyli np:
	*.iml
	out/*
	.idea/*
5. zapisujemy i wychodzimy
6. teraz przy upaniu tamto powinno byæ pomijane
7. jeœli chcemy coœ usun¹æ z serwera GitHub to:
	git rm --cached co_chcemy_usun¹æ -r
	np: git rm --cached .idea/* -r


Obs³uga ju¿ skonfigurowanego repozytorium:
git init - inicjalizacja
a) git add --all - dodanie wszystkich zmian
b) git commit -m "Jakie zmiany dodano w ty commicie"
c) git push - wrzucenie tego na serwer
a, b, c zawsze robi siê razem
git pull - pobranie repozytorium z serwera GitHub do naszego lokalnego na dysku, uwa¿aæ ¿eby sobie nie nadpisaæ czegoœ na dysku lokalnym
git rm --cached co_chcemy_usun¹æ -r -jeœli chcemy coœ usun¹æ z serwera GitHub